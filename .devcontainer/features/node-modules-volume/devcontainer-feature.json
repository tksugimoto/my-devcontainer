{
    "id": "node-modules-volume",
    "version": "1.0.0",
    "name": "Node Modules Volume Mount",
    "description": "Mounts node_modules as a named volume and sets proper ownership",
    "mounts": [
        {
            "source": "node_modules-${devcontainerId}",
            "target": "${containerWorkspaceFolder}/node_modules",
            "type": "volume"
        }
    ],
    "postCreateCommand": "sudo chown $(whoami) ${containerWorkspaceFolder}/node_modules"
}
